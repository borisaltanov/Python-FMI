class Vector:
    def __init__(self, values):
        self.values = values

    def add(self):
        sum = 0
        for i in range(len(values)):
            el = values[i]
            print (el)
            #if (isinstance(el, tuple)):
           # sum = el[i] + el[i+1] + el[i+2]
             #   return add(el[i])
            #else:
        #return sum


v = (1, 2, (3, 4, 5))
print (add(v))
